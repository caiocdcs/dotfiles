{{- $email := "caio@cdcs.dev" -}}
{{- $vaultwardenServer := "https://vault.int.cdcs.dev" -}}

[data]
    email = {{ $email | quote }}
    vaultwardenServer = {{ $vaultwardenServer | quote }}

[bitwarden]
    command = "bw"
